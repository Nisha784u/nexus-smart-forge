I have imported an existing project that was originally built with Lovable. I want you to take ownership of the existing codebase and turn it into a complete, polished, production-ready full-stack application.

IMPORTANT: First inspect and understand the entire existing project, its UI, components, routes, data flow, current functionality, dependencies, and folder structure. Do NOT rebuild the application from scratch and do NOT unnecessarily change the existing UI.

Please complete ALL of the following tasks:

### 1. Preserve and improve the existing design

* Keep the current overall UI/UX, layout, functionality, and visual identity.
* Do not remove existing features.
* Fix broken UI, responsiveness, spacing, alignment, navigation, buttons, forms, and obvious frontend issues wherever necessary.
* Make the application fully responsive for desktop, tablet, and mobile.
* Improve the design only where it genuinely improves usability and professionalism.
* Maintain a modern, clean, professional SaaS-style interface.
* Do not make random visual changes.

### 2. Replace all dummy/personal names

Search the ENTIRE codebase for placeholder/demo names such as:

* John
* Jane
* Michael
* David
* Sarah
* Emily
* Alex
* Robert
* William
  and other obviously fictional English demo names.

Replace them with natural Indian names appropriate to the context.

Use varied Indian names rather than repeatedly using the same name. Use realistic combinations of first and last names.

IMPORTANT:

* Do not change technical usernames, package names, URLs, email domains, API keys, environment variables, or code identifiers unless they are clearly dummy user-facing data.
* Check hardcoded data, cards, tables, profiles, testimonials, activity feeds, sample transactions, comments, notifications, and other UI content.

### 3. Update the application branding/content

Find the existing project/application name and replace placeholder branding where appropriate.

Make the application branding consistent across:

* Navbar
* Sidebar
* Dashboard
* Login/signup pages
* Browser/page titles
* Footer
* Empty states
* Notifications
* README
* Any visible user-facing content

Do not rename technical files or variables unnecessarily.

### 4. Build a REAL backend

The current project should become a proper full-stack application instead of relying only on frontend/mock data.

First identify what backend functionality the existing application actually requires.

Then implement the backend using the technology that best fits the existing project. Prefer:

* Node.js
* Express.js
* REST APIs
* MongoDB or PostgreSQL depending on the existing application's data model

Create a clean backend architecture with:

* routes
* controllers
* services where useful
* models/schema
* middleware
* validation
* error handling
* authentication
* authorization where required
* environment variables
* secure API handling

Do not create unnecessary complexity.

### 5. Replace mock/static data with database-backed data

Find all mock data, hardcoded arrays, fake API responses, demo records, and temporary local-state data that should actually be persistent.

Move appropriate application data to the database.

Implement proper CRUD operations:

* Create
* Read
* Update
* Delete

Connect the frontend to the real backend APIs.

Make sure:

* loading states work
* empty states work
* errors are handled
* successful operations update the UI correctly
* data persists after page refresh

Do not break existing frontend functionality while doing this.

### 6. Authentication and authorization

If the application has login/signup/user accounts, implement proper authentication.

Use secure authentication with:

* password hashing
* protected API routes
* authentication middleware
* session/token handling
* logout
* appropriate authorization

Never hardcode passwords, secrets, API keys, or credentials.

If authentication already exists, inspect it first and improve/fix it instead of replacing it unnecessarily.

### 7. Database

Set up the appropriate database structure based on the application's actual requirements.

Create proper schemas/models and relationships where necessary.

Add:

* database connection handling
* indexes where useful
* validation
* timestamps
* sensible defaults

Use environment variables for database credentials.

Create an example `.env.example` file without exposing any real secrets.

### 8. API integration

Connect every relevant frontend feature to the backend.

Remove unnecessary mock APIs and fake delays.

Use proper API requests and handle:

* success
* validation errors
* authentication errors
* server errors
* network errors
* loading states

Keep API URLs configurable through environment variables.

### 9. README.md

Completely rewrite the README so it accurately describes the FINAL application.

Include:

# Project name

## Overview

Explain what the application does and the problem it solves.

## Features

List the major implemented features.

## Tech Stack

Clearly mention:

* Frontend
* Backend
* Database
* Authentication
* APIs
* Other important libraries/services

## Project Structure

Explain the important folders.

## Installation

Give exact steps to install dependencies.

## Environment Variables

Explain every required environment variable using placeholders only.

## Running Locally

Provide exact frontend/backend commands.

## Database Setup

Explain how to configure and initialize the database.

## API

Document the important API endpoints.

## Authentication

Explain how authentication works at a high level.

## Deployment

Explain how the frontend and backend can be deployed.

## Future Improvements

Mention realistic future enhancements.

Do not write generic AI-generated filler. The README must describe the actual implementation in this repository.

### 10. Code quality

Clean up the project where necessary:

* remove unused imports
* remove dead code
* remove unnecessary console logs
* fix obvious warnings
* improve naming
* keep components modular
* avoid duplicated logic
* use reusable components
* add proper error handling
* keep secrets out of source code

Do NOT rewrite working code just for the sake of rewriting it.

### 11. Security

Check for:

* exposed API keys
* exposed database credentials
* hardcoded passwords
* insecure authentication
* unsafe input handling
* missing validation
* accidental secrets committed to Git

If any secret is found, do NOT display it. Replace it with an environment variable placeholder and explain what needs to be configured.

### 12. Testing and verification

After implementing the changes:

1. Install dependencies.
2. Run the application.
3. Run the build.
4. Check for TypeScript/JavaScript errors.
5. Check for lint/build errors where applicable.
6. Test important frontend flows.
7. Test backend API endpoints.
8. Test database operations.
9. Test authentication if applicable.
10. Fix errors you encounter.

Make sure the final application actually runs instead of only changing files.

### 13. GitHub

Keep the project GitHub-friendly.

Do not commit:

* `.env`
* passwords
* API keys
* database credentials
* private secrets

Ensure `.gitignore` is correct.

### VERY IMPORTANT WORKING RULES

* Do NOT delete the existing project.
* Do NOT rebuild everything from scratch.
* Do NOT unnecessarily change the existing UI.
* Do NOT remove existing features.
* Do NOT use fake backend implementations just to make the UI appear functional.
* Do NOT use hardcoded mock data where real persistence is required.
* Do NOT expose secrets.
* Before making major architectural changes, inspect the existing implementation and choose the least disruptive approach.
* If something already works correctly, preserve it.
* Prefer working, maintainable code over unnecessary complexity.

Start by auditing the entire repository and creating a concise implementation plan based on the ACTUAL codebase. Then implement the changes systematically.

At the end, provide:

1. What you changed
2. Backend architecture
3. Database used and schema/models created
4. Authentication implementation
5. API endpoints created
6. Frontend changes
7. Names/content changed
8. README changes
9. Environment variables I need to configure
10. Any remaining issues or manual steps
11. Exact commands to run the final project
